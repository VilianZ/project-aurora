// Copy this file to config.js and fill in your local values.
// config.js is gitignored.

window.AURORA_CONFIG = {
    // Leave both blank to run the dashboard in local/API-only mode.
    SUPABASE_URL: '',
    SUPABASE_KEY: '',

    // For Windows mobile hotspot, 192.168.137.1 is usually the laptop IP.
    SERVER_URL: 'http://192.168.137.1:8000',
    WS_BASE: 'ws://192.168.137.1:8000'
};
